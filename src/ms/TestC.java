package ms;

public class TestC {

    public static void main(String[] args) {
        System.out.println("--------");

        //owner_pet_quantity
        //df1 =df.na(col(name));
       // int count_name df.count();
       // df1 = df.na(col(gender));
      /*  String sql = """
                    select own, listagg(pet,',') wihtin group by ()
                
                """;*/

       /* Dataset<> countOfNAMe = df.sql("select name, count(*) from <> where name is null");
        Dataset<> geneder = df.sql"select geneder, count(*) from <> where geneder is null");
        Dataset<> job = df.sql"select job, count(*) from <> where job is null");*/
       //Dadatset datsaset = countOfNAMe.unino(fe).unionAll(job)





    }

}
